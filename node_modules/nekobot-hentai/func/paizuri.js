const superagent = require('superagent');

const paizuri = async () => {
    const body = await superagent.get("https://nekobot.xyz/api/image?type=paizuri")
    result = JSON.parse(body.res.text)
    if (!result) throw "Failed to get url from the api";
    return result
}

module.exports = paizuri